# Configuration pour le déploiement backend

# Environment
NODE_ENV=production

# Port (Railway l'assignera automatiquement)
PORT=3001

# Database PostgreSQL (Neon.tech ou ElephantSQL)
DB_HOST=your-neon-host.neon.tech
DB_PORT=5432
DB_NAME=your-database-name
DB_USER=your-username
DB_PASSWORD=your-password

# JWT Secret (minimum 32 caractères, généré aléatoirement)
JWT_SECRET=your-super-secure-jwt-secret-key-32-characters-minimum

# CORS (URL de votre frontend Netlify)
CORS_ORIGIN=https://your-app-name.netlify.app

# Optionnel : Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
