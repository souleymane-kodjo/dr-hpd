// src/layouts/AuthLayout.tsx
import { Outlet } from 'react-router-dom';

const AuthLayout = () => {
  // Potentiellement un conteneur MUI pour centrer le contenu
  return <Outlet />; 
};

export default AuthLayout;